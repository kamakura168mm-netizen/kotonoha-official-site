# 8/13・8/14公開後の中継URL切替・事前準備（2026-08-11作成・司令塔第28期発注）

対象2冊。公開当日、司令塔の合図で即実行する（kko_t02/koto_y10と同じ手順・2026-08-10に実施・完遂済み）。

## 対象1：koto_s08「寝る前の前向きなひとこと」#005
- 動画ID：`az2Jpd_szsw`（API実測確認済み・publishAt=2026-08-13T12:38:00Z＝JST 8/13 21:38・privacyStatus=private/uploadStatus=processed）
- 対象ページ：`docs/go/koto-s08-ch01/`〜`ch12/`（12件・現状は暫定でことのは英語チャンネルURLへ転送中）
- generate_site.pyの該当行（73行目付近）：
  ```python
  KOTO_S08_VIDEO_URL = PROVISIONAL_VIDEO_URL  # ← "https://youtu.be/az2Jpd_szsw" へ差し替え
  ```

## 対象2：kko_t03「別れと失恋で使う韓国語」#004
- 動画ID：`EdOmS7EN25Y`（API実測確認済み・publishAt=2026-08-14T12:38:00Z＝JST 8/14 21:38・privacyStatus=private/uploadStatus=processed）
- 対象ページ：`docs/go/kko-t03-ch01/`〜`ch12/`（12件・現状は暫定でことのは韓国語チャンネルURLへ転送中）
- generate_site.pyの該当行（64行目付近）：
  ```python
  KKO_T03_VIDEO_URL = KKO_PROVISIONAL_CHANNEL_URL  # ← "https://youtu.be/EdOmS7EN25Y" へ差し替え
  ```

## 実行手順（公開当日・合図が来たら）
1. 着手前に現在のページ状態を実機確認（二重実行防止）
2. 上記2行を書き換え
3. `python generate_site.py`
4. `git fetch origin`で remote差分確認（CNAME等の競合があれば司令塔へ一報・自己判断で解消しない）
5. `git add -A && git commit -m "..." && git push origin main`
6. `gh api repos/kamakura168mm-netizen/kotonoha-official-site/pages/builds/latest`でstatus確認（errored時は再ビルドAPI→それでもダメなら司令塔へ）
7. 対象24ページ（koto-s08-ch01〜12・kko-t03-ch01〜12）を実機HTTP確認（200＋本文中に新video_id含むこと）
8. 司令塔＋レーン進捗ログ.jsonlへ報告

このメモは切替完了後、削除して差し支えない。
